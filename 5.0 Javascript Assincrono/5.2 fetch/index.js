// const doc = fetch("./doc.txt");

// doc.then(r => r.text());

// const viaCep = fetch("/public/antilope.jpg");

// viaCep
//   .then(r => r.blob())
//   .then(body => {
//     const imageblob = URL.createObjectURL(body);

//     document.body.appendChild(document.createElement("img")).src = imageblob;
//   });
