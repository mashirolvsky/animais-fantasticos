const json = fetch("./dados.json");
// const jsonblock = document.querySelector(".json");
// json
//   .then(r => r.json())
//   .then(body => {
//     body.forEach(element => {
//       const span = document.createElement("span");
//       span.innerText = element.nome;
//       jsonblock.appendChild(span);
//     });
//   });
